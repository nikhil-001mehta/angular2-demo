import { Hero } from './hero';
//export var HEROES: Hero[]; this statement does not export anything since no value is assigned to HEROES
export var HEROES: Hero[] = []; //but this does export since value is assigned to the variable
